public class getterSetter {
    public static void main(String[] args) {

        /**
         * Implementa un programa donde un usuario debe rellenar
         * un array de int de tamaño 10.
         *
         * Se ha de controlar mediante el uso de excepciones que no se introduzcan
         * más de 10 elementos.
         ******* ayuda********
         * Hacéis bucle while o do while para que el user vaya introducinedo los datos.
         * Cuando se intente introducir un dato en una posición que no eista, controláis esa
         * excepción.
         *
         * Debéis implementar una función que compruebe que el número introducido es positivo.
         * Si no es positivo, lanza una excepción.
         * *******Ayuda********
         * Una función que recibe un número entero por parámetros.
         * NO DEVUELVE NADA.
         * La función se llama: comprobar
         *
         *
         *
         */
    }
}

/**
 * !userNuevo.equals(arrNombre[i])) {
 *
 *                         System.out.println("Introduce la contraseña que quieres registrar: ");
 *                         passNuevo = scan.next();
 *                         System.out.println(ANSI_GREEN+"Usuario registrado."+ANSI_RESET);
 *                         //Con esto debería quedar registrado el nuevo usuario con su contraseña...
 *                         //Pero no parece funcionar del todo. No sé por qué.
 *                         arrNombre[i] = userNuevo;
 *                         arrPass[i] = passNuevo;
 *
 *                         break;
 */
